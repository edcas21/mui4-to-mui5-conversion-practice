What does it do?
Allows you to create mock API. Can be used to build a quick back-end for prototyping and mocking.

installation:
npm install -g json-server

local use on project:
npx json-server --watch <path db json file> --port <port number>